<%= form_for(compare_line) do |f| %>
  <% if compare_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compare_line.errors.count, "error") %> prohibited this compare_line from being saved:</h2>

      <ul>
      <% compare_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
